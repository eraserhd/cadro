== Arbor


== Development

Start the REPLs and build everything for debug.

----
$ clj -M:dev:shadow-cljs watch app
----

Run tests:

----
$ rep '(refresh) (run-tests)'
----

Testing on tablet:

----
$ clj -M:shadow-cljs release app
$ npx cap run android
----
